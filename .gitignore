# --- Python ---
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- macOS ---
.DS_Store

# --- IDEs ---
.idea/
.vscode/

# --- Streamlit ---
.streamlit/secrets.toml

# --- 환경변수 ---
.env
!.env.example

# --- Claude Code local config ---
.claude/

# --- 외부 참고용 repo (분석 전용, 통합 안 함) ---
PIKIT-BE-develop/
PIKIT-GameServer-develop/
PIKITbot/

# --- DATA — never commit raw beta data ---
# Local data folder used for development:
PIKIT BETA DATA/
PIKIT_BETA_DATA/

# In-repo private data folder (used in production via PIKIT_DATA_ROOT=./data):
data/

# Misc CSV / archives that might end up at the repo root:
*.zip
/*.csv
/*.json
# Exception: bot_state.json 은 sanitized 봇 세트 템플릿 (privateKey 없음). 커밋 OK.
!bot_state.json

# Anything dropped into a top-level snapshot folder pattern:
/2026.*
/2027.*
